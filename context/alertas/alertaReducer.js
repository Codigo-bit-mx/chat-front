import {

    
} from '../../types/index';


const alertaReducer = ( state, action ) => {
    switch(action.type) {




        default:
            return state;
    }
}


export default alertaReducer;